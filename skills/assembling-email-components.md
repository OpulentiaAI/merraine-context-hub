---
type: mcp.skill::au-mcp-sdk
name: assembling-email-components
description: "Fill the named parts of one outbound email. Use when a draft is a blob and needs from, subject, preview, first line, action, and plain text. Triggers: email components, subject and preview, plain text version."
---

# Assembling email components

Open [[email-components]]. Fill every heading or leave `[NEED]`.

Write the body first, then subject and preview together. Read them at
truncation length, as one inbox row. One action. Handwritten plain text.
