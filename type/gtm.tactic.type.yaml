#: A play borrowed from outside research. Always attributed.
extends: [idea::au-base-types, gtm.managed]
fields:
  #: Author or repo.
  source: String
  sourceUrl: String
  category: [sourcing, enrichment, copy, sequencing, events, ops, architecture]
  appliesTo?: gtm.signal*[]
  #: Quote the author's claim. Never restate it as our own number.
  claimedResult?: String
  adoptionState: [adopted, adapted, rejected, backlog]
body:
  - section: The play
  - section: How we apply it for Merraine
