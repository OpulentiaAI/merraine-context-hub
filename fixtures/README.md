# Synthetic validation fixtures

Every fixture in this directory is synthetic and contains no real person,
company, or contact detail. `good/` contains valid examples; `bad/` contains
deliberately invalid examples with the validator error each must produce.

