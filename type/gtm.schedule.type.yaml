#: When an automation fires.
extends: [idea::au-base-types, gtm.managed]
fields:
  kind: [cron, event, webhook, manual]
  #: Cron string, or the event/webhook name.
  expression: String
  timezone: String
  #: parallel-monitor | opulent-native | mcp | manual
  source: String
