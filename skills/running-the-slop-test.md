---
type: mcp.skill::au-mcp-sdk
name: running-the-slop-test
description: "Rewrite a draft until it sounds like Jeremy. Use when asked to edit copy, check slop, or whether a note is sendable. Triggers: slop, rewrite this, does this sound like me, too salesy."
---

# Running the slop test

Read [[slop]] and [[slop-patterns]]. Mark trips. Rewrite or fail closed.

Two trips means rewrite. Never mark `sendReady: yes`.
